import React, { Component } from 'react';
import Includer from '../includer/includer';

const HomeScreen = () => {
  return (
    <div className="home">
      <Includer />
    </div>
  );
}

export default HomeScreen;